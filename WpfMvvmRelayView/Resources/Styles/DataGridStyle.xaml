<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--DataGrid-->
    <SolidColorBrush x:Key="HeaderColorBrush" Color="Blue"/>
    <SolidColorBrush x:Key="AltBrush1" Color="#DDC3C3C3"/><!--Color="#FFC3C3C3"-->
    <SolidColorBrush x:Key="AltBrush2" Color="#FFD3D3D3"/>
    
    <!-- DataGrid Style -->
    <Style TargetType="{x:Type DataGrid}">
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="GridLinesVisibility" Value="None" />
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="CanUserResizeRows" Value="True" />
        <Setter Property="CanUserResizeColumns" Value="True" /> <!-- Enable column resizing -->
        <Setter Property="RowDetailsVisibilityMode" Value="VisibleWhenSelected"/>
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="AlternationCount" Value="2"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Visible"/>
        <Setter Property="EnableColumnVirtualization" Value="False"/>
        <Setter Property="Background" Value="LightGray"/>
        <Setter Property="VerticalGridLinesBrush" Value="Black"/> <!-- Ensure vertical grid lines are defined -->
        <Setter Property="HorizontalGridLinesBrush" Value="Transparent"/>
    </Style>
    
    <!-- DataGridColumnHeader Style -->
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Grid Background="{StaticResource HeaderColorBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/> <!-- For the vertical grid line -->
                        </Grid.ColumnDefinitions>
                        <TextBlock 
                            Grid.Column="0"
                            Height="30"
                            Padding="15, 8, 15, 0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="0, 0, 0, 0"
                            FontFamily="/Fonts/#Rubik Medium"
                            FontSize="15"
                            FontWeight="ExtraBold"
                            Text="{TemplateBinding Content}"
                            Foreground="Black"/>
                        <!-- Vertical Grid Line -->
                        <Border 
                            Grid.Column="1"
                            Width="1"
                            Padding="0, 0, 0, 0"
                            Background="{Binding VerticalGridLinesBrush, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                            VerticalAlignment="Stretch"/>
                        <!-- Thumb for Resizing -->
                        <Thumb 
                            x:Name="PART_ColumnResizer"
                            Grid.Column="1"
                            Width="2"
                            Cursor="SizeWE"
                            VerticalAlignment="Stretch"
                            Background="Black"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <!-- Ensure the header supports resizing -->
        <Setter Property="Margin" Value="0 0 8 0" />
        <Setter Property="Padding" Value="10, 0, 10, 0"/>
    </Style>
    
    <Style TargetType="{x:Type DataGridRow}">
        <Setter Property="Height" Value="30"/>
        <Setter Property="Margin" Value="0 0 " />
        <Setter Property="Background" Value="{StaticResource AltBrush1}"/>
        <Setter Property="SnapsToDevicePixels" Value="true"/>
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
        <!--<Setter Property="FontFamily" Value="/Fonts/#Roboto Bold"/>-->
        <Setter Property="ValidationErrorTemplate">
            <Setter.Value>
                <ControlTemplate>
                    <TextBlock Foreground="Red" 
                               Height="30"
                               Margin="0,0,0,0" 
                               Text="!" 
                               VerticalAlignment="Center"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridRow}">
                    <Grid>
                        <Border x:Name="DGR_Border" 
                                Padding="0 5" 
                                CornerRadius="0" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="1" SnapsToDevicePixels="True">
                            <!--<Border.BorderBrush>
                                <SolidColorBrush Color="Black"/>
                            </Border.BorderBrush>-->
                            <SelectiveScrollingGrid>
                                <SelectiveScrollingGrid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </SelectiveScrollingGrid.ColumnDefinitions>
                                <SelectiveScrollingGrid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </SelectiveScrollingGrid.RowDefinitions>
                                <DataGridCellsPresenter Grid.Column="1" ItemsPanel="{TemplateBinding ItemsPanel}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                <DataGridDetailsPresenter Grid.Column="1" Grid.Row="1" SelectiveScrollingGrid.SelectiveScrollingOrientation="{Binding AreRowDetailsFrozen, Converter={x:Static DataGrid.RowDetailsScrollingConverter}, ConverterParameter={x:Static SelectiveScrollingOrientation.Vertical}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" Visibility="{TemplateBinding DetailsVisibility}"/>
                                <DataGridRowHeader Grid.RowSpan="2" SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical" Visibility="{Binding HeadersVisibility, Converter={x:Static DataGrid.HeadersVisibilityConverter}, ConverterParameter={x:Static DataGridHeadersVisibility.Row}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>
                            </SelectiveScrollingGrid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=AlternationIndex}" Value="1">
                <Setter Property="Background" Value="{StaticResource AltBrush2}"/>
            </DataTrigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="Gray"/>
                <Setter Property="Foreground" Value="Black"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <!--<Setter Property="Background" Value="#623ED0"/>-->
                <Setter Property="Background" Value="Green"/>
                <Setter Property="Foreground" Value="Black"/>
            </Trigger>
            <!--<MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="true"/>
                    <Condition Property="Selector.IsSelectionActive" Value="false"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="#dfdfdf"/>
            </MultiTrigger>-->
            
        </Style.Triggers>
    </Style>
    
    <!-- Style for EmojiTextBlock in DataGrid cells -->
    <!--<Style TargetType="emoji:TextBlock" x:Key="DataGridCellEmojiTextBlockStyle">
        <Setter Property="FontFamily" Value="Segoe UI Emoji"/>
    </Style>-->
    <!--<DataTemplate x:Key="EmojiCellTemplate">
        <emoji:TextBlock Text="{Binding}"/>
    </DataTemplate>-->
    
    <Style TargetType="{x:Type DataGridCell}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Margin" Value="10 0 10 0" />
        <Setter Property="Padding" Value="10, 0, 10, 0"/>
        <Setter Property="BorderThickness" Value="0,0,1,0"/>
        <Setter Property="BorderBrush" Value="Gray"/>
        <!--<Setter Property="HorizontalAlignment" Value="Center"/>-->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridCell}">
                    <Border Background="{TemplateBinding Background}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            SnapsToDevicePixels="True">
                        <ContentPresenter VerticalAlignment="Stretch" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!--DataGrid-->
</ResourceDictionary>