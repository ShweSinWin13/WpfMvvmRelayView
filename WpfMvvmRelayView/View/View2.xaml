

<UserControl x:Class="WpfMvvmRelayView.View.View2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WpfMvvmRelayView.ViewModel"
             xmlns:icon = "http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Background="#EFF2F7">
    <UserControl.DataContext>
        <vm:View2VM/>
    </UserControl.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/> 
        </Grid.RowDefinitions>
        
        <!-- Page Title -->
        <TextBlock Text="View 2" Grid.Row="1" FontSize="22" FontWeight="SemiBold" Margin="5 20 0 0" Foreground="#121518" HorizontalAlignment="Left"/>
        
        <!-- Add Button -->
        <Button Style="{StaticResource AddButton}">
            <StackPanel Orientation="Horizontal">
                <icon:PackIconMaterial Kind="Plus" VerticalAlignment="Center" Margin="0 1 8 0" Width="11" Height="11"/>
                <TextBlock Text="Add" FontWeight="SemiBold"></TextBlock>
            </StackPanel>
        </Button>
        
        <!-- List Data Grid -->
        <DataGrid Grid.Row="2" Margin="0 10 0 0">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="C" CanUserResize="False" Width="Auto" ElementStyle="{StaticResource CheckBoxStyle1}">
                    <DataGridCheckBoxColumn.HeaderTemplate>
                        <DataTemplate>
                            <CheckBox Style="{StaticResource CheckBoxStyle1}"></CheckBox>
                        </DataTemplate>
                    </DataGridCheckBoxColumn.HeaderTemplate>
                </DataGridCheckBoxColumn>
                
                <!-- List -->
                <DataGridTemplateColumn Header="List" IsReadOnly="True" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Border Width="25" Height="25" CornerRadius="50" Margin="0 10 0 0" Background="{Binding BgColor}">
                                    <TextBlock Text="{}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" Margin="0 0 0 1"/>
                                </Border>
                                
                                <TextBlock Text="" VerticalAlignment="Center"></TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        
        <!--
        <Grid Row="0">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="View 2 (ListView)"/>
        </Grid>
        
        <Grid Row="1">
            <Menu>
                <MenuItem
                    Header="Edit (Menu)">
                    <MenuItem
                        Header="Delete"
                        Command="{Binding DeleteCmd}"
                        CommandParameter="{x:Reference listView}">
                    </MenuItem>
                </MenuItem>
            </Menu>
        </Grid>
        
        <Grid Row="2">
            <ListView
                x:Name="listView"
                ItemsSource="{Binding Data}"
                SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"/>
        </Grid>
        
        <Grid Row="3">
            <StackPanel
                Orientation="Horizontal">
                <TextBox
                    x:Name="tb"
                    Width="100"/>
                <Button
                    Content="Add"
                    Command="{Binding AddCmd}"
                    CommandParameter="{x:Reference tb}"/>
            </StackPanel>
        </Grid>
        !-->
    </Grid>
</UserControl>